import { readFileToArray } from "../../utils/readFileToArray";

export const d12c2 = (input?: string[]) => {
  console.log("You have selected Day 12 Challenge 2");
  const rows = input ? input : readFileToArray("input", 12);

  let answer;

  return answer;
};
