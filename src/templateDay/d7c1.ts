import { readFileToCharArr } from "../../utils/readFileToCharArr";

// change var name
export const d7c1 = (input?: string[]) => {
  // change days and challenges
  console.log("You have selected Day 7 Challenge 1");
  // change file number
  const values = input ? input : readFileToCharArr("input", 7);

  let answer = 0;

  return answer;
};
