import { readFileToArray } from "../../utils/readFileToArray";

export const d11c2 = (input?: string[]) => {
  console.log("You have selected Day 11 Challenge 2");
  const rows = input ? input : readFileToArray("input", 11);

  let answer;

  return answer;
};
