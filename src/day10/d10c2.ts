import { readFileToArray } from "../../utils/readFileToArray";

export const d10c2 = (input?: string[]) => {
  console.log("You have selected Day 10 Challenge 2");
  const rows = input ? input : readFileToArray("input", 10);

  let answer;

  return answer;
};
